# Changelog

## [0.6.0-alpha] - 2026-09-01

### 桌面安全边界

- 主窗口加入 CSP、精确的开发源与打包文件根目录校验，默认拒绝非必要 Electron 权限，并禁止 WebView 与拖放导航。
- 外部导航只接受无凭据 HTTPS；更新下载入口进一步限定为 GitHub Release 页面。
- QQ 空间图片下载会在重定向后再次校验媒体域名，只接受安全位图类型，并同时执行 Content-Length 预检与 80 MB 流式硬上限。
- 采集启动前检查保存位置至少有 128 MB 可用空间；写入过程中遇到磁盘耗尽会停止任务并保留恢复点。

### 无障碍与缩放

- 对话框统一圈定键盘焦点，支持 Esc 关闭并在关闭后将焦点还给原操作；图片查看器继续保留方向键与缩放快捷操作。
- 表单、选择框、文本区和自定义焦点目标统一显示清晰焦点环，新增 Windows 强制高对比度样式。
- 将 8–10px 辅助文字提高到最低 11px，并修复字号调整后的 AI 模型选择与重新生成按钮对齐。
- 修复标题栏响应式选择器优先级，在 200% 缩放下不再产生横向溢出；新增自动化缩放回归。

### 更新与发行工程

- 设置 → 关于新增 GitHub 版本检查，显示是否存在新版本及校验值状态；仍由用户手动下载，不在无代码签名时执行静默更新。
- 加入正式应用图标和当前用户安装配置；GitHub Actions 可在配置证书后签名构建。
- 新增标签触发的 Windows Release 工作流，自动运行全量回归、生成安装版与免安装版，并附带 SHA-256、CycloneDX SBOM 和生产依赖许可清单。
- 新增 URL、权限、媒体限制、版本排序、低磁盘、中文路径、Release 配置和 200% 缩放回归。

### 尚未解除的 Alpha 门槛

- 当前开发环境没有可信 Windows 代码签名证书，自动安装更新与失败回滚因此保持禁用。
- Windows 10/11 干净系统、SmartScreen 信誉和 WPS 打开 DOCX 仍需真实环境人工验收。

## [0.5.0-alpha] - 2026-09-01

### 多格式档案导出

- 档案页新增 HTML、PDF、DOCX 导出；HTML 为内嵌配图的单文件离线档案，PDF 使用 A4 打印排版，DOCX 可继续在 Microsoft Word 中编辑。
- 支持全部档案、当前搜索与分类、指定日期范围，以及原图、压缩副本或不含配图三种媒体策略。
- 评论与点赞可分别选择；默认匿名化好友昵称和正文中的好友提及，关闭匿名化且包含互动人员时必须再次确认。
- 匿名好友编号改为每篇说说独立从“好友 1”开始；同篇内评论者、点赞者与完整 `@昵称` 共用身份映射，复杂符号昵称不再被拆成多个编号。
- HTML、PDF、DOCX 中的 QQ 表情改为 `/捂脸`、`/比心` 等语义名称；未知代码保留为 `/QQ表情 e编号`，不再统一丢失为“QQ表情”。
- 导出由 Electron 主进程定位当前账号档案并打开系统保存窗口，渲染层不能指定任意文件路径；外链仅允许 HTTPS，HTML 使用离线 CSP 且不包含脚本。
- 长时间导出显示阶段文案、旋转状态、确定进度和完成摘要，系统保存窗口取消后可立即返回选项页。
- 新增导出选项、匿名化、媒体路径安全、离线 HTML、标准 DOCX、PDF 页面边界和 Microsoft Word 实际打开/渲染回归。

### 范围说明

- 本版本只导出已经保存到本地的档案，首个正式版仍只承诺说说正文及说说配图；完整相册、视频和独立日志采集继续延期。
- WPS 未在当前开发机安装，DOCX 已通过标准 OOXML 结构与 Microsoft Word 验证，仍保留 WPS 人工兼容检查项。

## [0.4.1-alpha] - 2026-09-01

### 界面与可靠性收尾

- 修正浏览器演示中“关于”页面仍显示旧版本号的问题；“检查更新”改为符合实际行为的“查看新版本”。
- 收紧首页、空档案和账号删除提示，不再暗示未实现的全空间采集或仍保留临时登录会话。
- 档案详情能够容忍旧档案中的未知内容类型，并忽略非 HTTPS 或格式损坏的外部链接，避免单条异常记录中断整个页面。

## [0.4.0-alpha] - 2026-08-31

### 版本整理

- 按 `vMAJOR.FEATURE.FIX[-alpha]` 规则，将 `v0.3.1-alpha` 之后尚未单独发布的开发改动统一收录为 `v0.4.0-alpha`，不再保留 `v0.3.2` 至原 `v0.6.0` 的中间公开版本号。

### 点赞名单补充采集

- 勾选“点赞记录”后，在正文采集完成后通过独立的本人说说点赞名单接口逐条补充可见点赞者；请求按顺序执行并加入间隔，避免并发触发 QQ 限制。
- 每补充一条就原子写入档案和恢复点；会话失效、限流或接口异常时保留已完成结果，下次备份从恢复点继续，不回退正文和已有互动数据。
- 点赞总数与当前可见名单继续分开保存；档案只记录昵称，不保存点赞者 QQ 号，并在 QQ 未返回完整名单时明确提示。

### 互动详情排版

- 详情页直接展示已取得的点赞者昵称，不再只有人数而缺少人员信息。
- 评论昵称限制为单行省略，正文紧随昵称自然换行，避免长昵称形成狭窄的多行列；整体仍沿用暖纸张档案样式。
- 新增点赞接口解析、分页、限流、恢复点和真实 Electron 互动详情回归。

### 开发阶段：准确性与大账号性能

#### 归档准确性

- 将 QQ 返回的评论、点赞总数与当前响应中实际展开的人员详情分开保存和展示，避免用可见数组长度冒充总数。
- 评论作者统一使用 `authorName`；归档规范化会移除旧别名和无关 UIN，界面、搜索与 AI 上下文共享同一字段约定。
- 解析器版本升级到 7，已有旧版记录会按可回滚迁移规则在下次采集时重新规范化。

#### 大档案性能

- 新增可重建的增量条目索引，汇总统计、年份范围、分页搜索不再反复读取全部记录文件。
- 媒体索引改为内存缓存与分页批量原子落盘，降低大量图片采集时的磁盘读写放大。
- 主进程提供受限分页读取，档案时间线使用带预加载的虚拟滚动；AI 读取真实档案时限制为最多 300 条相关记录。
- 时间线继续跟随档案页面统一滚动，不再在左栏制造第二条独立滚动条。
- 新增 1 万条动态、5 万个媒体引用的压力回归，覆盖索引重建、分页和搜索。

#### 兼容性与状态

- 采集完成结果新增“正常”“兼容模式”“部分完成”三类适配器状态，明确区分说说分类主路径、本人时间线降级路径和历史截断。
- 建立 QQ 兼容性矩阵，记录自动化覆盖、目前大小账号真实观察及正式发布前仍需补齐的人工验证门槛。
- 完整性检查改为按需执行，打开档案不再为每次翻页扫描全部记录；单独的“检查与修复”仍以每条记录为事实来源重建索引。

### 开发阶段：账号资料与图片查看器

#### 账号资料

- 账号入口改为显示本人 QQ 头像与昵称，账号列表副标题显示完整 QQ 号；已有档案会从清单和本人动态作者字段补全资料，空间标题同步使用昵称。
- 登录成功后通过 QQ 空间资料响应补充昵称，头像使用由本人 QQ 号生成的腾讯头像地址；任一资料暂时不可用时保留本地占位。
- 账号注册表升级到版本 3，只持久化本人 QQ 号、昵称和可重新生成的官方头像地址；临时登录 Cookie 仍在每次采集结束后清除，脱敏诊断仍排除完整 QQ 号。

#### 验证

- 新增空间资料 JSONP 安全解析、账号资料持久化、头像回退、完整 QQ 号展示和旧档案身份补全回归。
- 新增账号菜单视觉截图回归，验证顶部入口和下拉列表的头像、昵称及两行信息布局。

#### 图片查看器

- 大图按顶部工具栏与底部缩略图之间的安全区域等比适配，纵向较长的图片不再越出窗口。
- 支持以鼠标位置为中心的滚轮缩放、放大后拖动、双击或按 `0` 恢复适应窗口，并提供可点击的缩放控制组。
- 左右切图箭头固定在图片区域的垂直中心；拖动边界按图片实际显示尺寸计算，最高缩放时也无法把整张图片拖出视野。
- 新增原生鼠标点击、箭头居中、500% 极限拖动和 1120 × 720 截图回归。

### 开发阶段：按分类采集与好友流隔离

#### 修复

- 说说采集改为优先读取 QQ 空间“说说”分类的 `emotion_cgi_msglist_v6`，按返回的总数与数值偏移分页，不再把好友动态页数当作本人说说页数。
- 从代码层禁止 `scope=0` 好友动态流进入个人归档；分类接口首页遇到 `-10000` 限流时只允许退到 `scope=1` 本人时间线，后续失败则保存恢复点并标记部分完成。
- 新解析器保留图文说说、转发正文与安全外链、评论回复、可见点赞者及官方统计，解析器迁移版本升级到 5。
- 进度文案明确显示“说说分类第几页”和分类总条数；无法取得总数的本人时间线回退会明确说明不会扫描好友动态。
- 修复单张图片在全屏查看器中缩成角落缩略图的问题；单图现在使用完整舞台并隐藏无意义的缩略图栏。
- 过长说说改为在详情纸张内滚动并强制安全换行，不再把页面整体撑出窗口。
- 详情纸张取消固定像素高度封顶，改为实时测量卡片顶部到窗口底部的可用空间并保留 12px 安全间距；高窗口不再留下大面积空白，内容接近底部后才在卡片内滚动。
- 没有内部滚动条的短详情不再拦截滚轮事件，在卡片上滚动会自然带动整个档案页面。
- 修复图片或字体完成排版后仅多出少量高度时，评论贴住窗口底部却不出现滚动条的问题；原生自动溢出和内容变化监听现在会覆盖这一临界状态。
- 保留并渲染说说、评论中的 `[em]eNNN[/em]` QQ 表情编号，优先读取腾讯官方表情资源，资源不可用时显示本地兜底图标。
- 将说说、评论中的 QQ 内部好友标记 `@{uin:...,nick:...}` 规范为 `@昵称`；旧档案显示时同步脱敏，缺少昵称的异常标记显示为“@QQ好友”。

#### 验证

- 新增分类接口、发布者过滤、图片、转发、外链、评论、数值分页、限流降级及禁止好友流测试。
- 新增单图居中、长文约束、表情代码隐藏与官方表情资源地址的桌面渲染回归。
- 新增正文、评论、含空格或标点昵称及缺失昵称场景的好友标记解析与桌面渲染回归。
- 通过归档、独立采集进程、QQ 会话、桌面交互、AI 兼容、Sites worker 与生产构建回归。

### 开发阶段：发布者字段与部分完成修复

#### 修复

- 修复 `scope=1` 响应中 `rawItem.opuin` 不代表时间线发布者时，本人说说全部被过滤并显示 0 条的问题；发布者现在优先取正文前的发布者名片与 `feed_data.uin`，原作者单独取 `data-origuin`。
- 修复解析器迁移回滚后记录文件存在、但清单计数错误恢复为 0，导致主进程不建立档案索引的问题。
- 主进程在失败或取消后以磁盘实际记录重新汇总，不再信任可能过期的工作进程计数。

#### 兼容性

- 个人时间线的后续页被 QQ 拒绝时，自动尝试 `scope=0 + 本人过滤` 的兼容读取路径。
- 若兼容路径仍无法取得更早内容，合并本轮新记录与旧档案、标记为“部分完成”并立即开放查看，不再把已取回内容全部回滚或伪装成完整备份。
- 新增发布者字段回归、磁盘计数恢复、迁移合并与部分完成流程测试。

### 开发阶段：大号分页与转发兼容修复

#### 修复

- 修复转发说说中 `feed_data.uin` 指向原作者时，本人发布的转发被误判为他人动态并过滤的问题。
- 保留转发卡片中的安全 HTTPS 外链，并在档案详情中显示可打开的视频或网页链接。
- 大账号采集改为每页 20 条并放慢连续翻页；中途游标遇到 `-10001` 时仅延迟重试一次，降低 QQ 风控误判为登录失效的概率。
- 采集失败或取消前已经写入的内容会登记到当前账号，并立即出现在“我的档案”，不再出现“保存了若干条但界面看不到”的情况。

#### 数据兼容

- 解析器升级到版本 4；旧记录在重新采集成功前仍按事务式迁移规则保留，可失败回滚。
- 新增转发发布者、外链、保守分页和有限重试的回归测试。

## [0.3.1-alpha] - 2026-08-30

### 修复

- 修复旧分页恢复点被 QQ 拒绝时误报“QQ 会话需要刷新”的问题。
- 恢复后的首个请求若因旧游标返回鉴权错误，会自动从第一页重新扫描；稳定内容 ID 会阻止重复保存。
- 只有无游标的新请求仍返回鉴权错误时才要求重新扫码，避免在过期恢复点上反复登录。
- 新增恢复游标重置的脱敏诊断与回归测试。

## [0.3.0-alpha] - 2026-08-30

### 账号与隐私

- QQ 登录改为仅供单次采集使用的临时隔离会话；采集完成、失败或取消后自动清除，并在升级时清理旧版持久会话。
- 顶部账号入口现在用于切换各 QQ 的本地档案；每个账号右侧提供删除全部数据入口。
- 删除前进行二次确认并展示目标路径；应用只会删除已授权目录内、具有有效归档标识且未被其他账号共用的档案，删除时移入系统回收站。
- 删除 AI 模型服务时同步删除其加密 API Key，不再提供含义宽泛的“清除敏感数据”。
- 新增字段白名单式脱敏诊断包导出，排除 Cookie、完整 QQ 号、API Key、绝对路径、档案正文和原始 QQ 响应。

### 界面与操作

- 账号菜单区分“已保存档案”与待备份状态，并在删除确认中明确说明影响范围和可恢复方式。
- 备份完成页明确提示临时 QQ 会话已经自动清除。

## [0.2.1-alpha] - 2026-08-30

### 界面调整

- 将“检查与修复”从档案页标题区域移动到“设置 → 常规”。
- 新入口会说明该功能只检查和修复本地档案，不会修改 QQ 空间内容。
- 当前账号没有真实本地档案时禁用按钮；检测到问题时显示损坏记录与媒体问题数量。

## [0.2.0-alpha] - 2026-08-30

### 新功能

- 新增真正的增量备份：记录说说高水位与最近内容 ID，展示新增、更新和跳过数量。
- 新增档案完整性检查与修复入口，可隔离损坏记录并标记缺失图片等待重新下载。
- 内容发生变化时保存旧版归档记录，便于恢复与问题排查。

### 数据规则与可靠性

- 同一来源动态使用稳定文件名，重复出现不会生成重复条目。
- QQ 接口中暂时不可见或疑似删除的内容继续保留在本地，不根据单次缺失自动删除。
- 近期备份遇到整页已知内容后提前停止；每 30 天至少执行一次完整扫描，以检查较早内容的变化。
- AI 服务配置与应用偏好均使用临时文件加原子替换，降低异常退出导致配置损坏的风险。

## [0.1.1-alpha] - 2026-08-30

### 修复

- 首页“上次备份”不再固定显示“刚刚”，会按最后一次成功备份时间自动更新。
- 失败或取消的采集不会错误刷新最后备份时间。

### 改进

- 解析器升级改为事务式迁移；登录失效、采集失败或取消时自动恢复旧档案。
- 应用在迁移过程中异常退出后，会在下次启动时自动回滚未完成事务。
- 成功迁移后仍将旧版记录保留在档案诊断目录，便于恢复和排查。
- 新增正式版优化清单，按 Alpha、基本可用版本和 v1.0 发布门槛推进。

## [0.1.0-alpha] - 2026-08-29

首个公开 Alpha 版本。

### 已加入

- Windows Electron 桌面壳、安装版与免安装版。
- QQ 官方页面扫码登录及多账号独立会话。
- 说说正文、配图、内嵌评论和可见点赞者的分页采集。
- 本地版本化归档、媒体去重、原子写入、诊断与恢复点。
- 档案搜索、分类、详情、图片网格和全屏浏览。
- 多模型服务配置、AI 回顾和限定档案范围的提问。

### Alpha 限制

- 尚未实现完整相册、视频、独立日志和多格式导出。
- QQ 非公开接口可能随服务端变化。
- 安装包尚未进行代码签名。

[0.6.0-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.6.0-alpha
[0.5.0-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.5.0-alpha
[0.4.1-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.4.1-alpha
[0.4.0-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.4.0-alpha
[0.3.1-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.3.1-alpha
[0.3.0-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.3.0-alpha
[0.2.1-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.2.1-alpha
[0.2.0-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.2.0-alpha
[0.1.1-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.1.1-alpha
[0.1.0-alpha]: https://github.com/Socialist-Sister/qzone-journal/releases/tag/v0.1.0-alpha
