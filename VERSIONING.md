# 版本号规则

本项目使用 `v主版本.功能版本.修复版本[-alpha]`。

## 数字含义

- 第一位：主版本。发生正式的大范围产品阶段变化或不兼容升级时增加。
- 第二位：功能性更新。新增主要功能、明显扩展能力或改变核心工作流时增加。
- 第三位：修复性更新。用于 Bug 修复、兼容性修复、小幅体验完善和不改变主要功能范围的内部改进。

功能版本增加时，修复版本归零。例如：`v0.1.4-alpha → v0.2.0-alpha`。

## Alpha 标记

- 带 `-alpha`：早期测试版本，不保证能够正常使用，也不应作为唯一备份手段。
- 不带 `-alpha`：已经通过该版本范围内的基本回归，基本保证程序能够正常完成承诺的核心流程。

同一功能版本验证稳定后，可以从预发布进入不带 Alpha 的版本，例如：

```text
v0.2.0-alpha
v0.2.1-alpha
v0.2.2
```

这里的 `v0.2.2` 表示经过两个修复版本后，该功能线达到基本可用标准；并不要求必须发布一个同号的 `v0.2.2-alpha`。

## 当前约定

- 暂不使用 Beta、RC 等额外后缀，避免与“是否基本保证可用”产生重复含义。
- Git 标签、GitHub Release、安装包、免安装包和应用内版本号保持一致。
- 版本号只描述变更范围和可用性承诺，不用于表示开发完成百分比。
