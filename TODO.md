# 正式版优化清单

目标：先把“自己的 QQ 空间说说可靠备份到本地”做稳，再扩展相册、日志、导出和更完整的正式发行能力。

优先级说明：`P0` 阻挡去除 Alpha 标记，`P1` 阻挡 v1.0，`P2` 可在正式版后继续增强。版本规则见 [VERSIONING.md](VERSIONING.md)。

## v0.1.1-alpha：时间与迁移安全

- [x] `P0` “上次备份”使用最后一次成功备份时间，并随时间自动刷新。
- [x] `P0` 解析器迁移改为事务式流程：登录或采集失败后自动恢复旧档案。
- [x] `P0` 应用异常退出后自动恢复未完成的迁移事务。

## v0.2.0-alpha：增量备份与档案恢复

- [x] `P0` 真正的增量备份：保存最新内容高水位，统计新增、更新和跳过数量。
- [x] `P0` 定义内容修改、删除和重复记录的处理规则。
- [x] `P0` 增加档案完整性检查、损坏记录隔离和“修复档案”入口。
- [x] `P0` AI 配置与应用偏好改为原子写入。

## v0.2.1-alpha：档案维护入口调整

- [x] 将“检查与修复”移动到“设置 → 常规”，并说明只处理本地档案。
- [x] 没有真实档案时禁用操作，发现问题时显示异常数量。

## v0.3.0-alpha：账号与数据控制

- [x] `P0` QQ 登录改为临时会话；采集完成、失败或取消后自动清除，并迁移清理旧版持久会话。
- [x] `P0` 顶部账号入口统一承担本地档案切换与管理，不再提供含义重叠的“退出登录”和“移除账号”。
- [x] `P0` 每个账号提供删除全部本地数据的垃圾桶入口；二次确认展示目标路径，归档经安全校验后移入系统回收站。
- [x] `P1` 删除 AI 模型服务时同步删除该服务在本机加密保存的 API Key，不增加独立的“清除敏感数据”。
- [x] `P1` 增加脱敏诊断包导出，不包含 Cookie、完整 QQ 号、API Key、绝对路径、档案正文和原始 QQ 响应。

## v0.3.1-alpha：恢复点兼容修复

- [x] `P0` 已保存分页游标被 QQ 拒绝时，仅在恢复后的首个请求自动放弃旧游标并从第一页安全重扫。
- [x] `P0` 空游标请求仍返回鉴权错误时才要求重新扫码，避免把过期恢复点误报成登录失效。
- [x] `P0` 为游标重置增加脱敏诊断和定向回归测试；稳定内容 ID 继续负责去重。

## v0.4.0-alpha：采集兼容、账号资料、互动详情与大账号性能

- [x] `P0` 修正转发说说的发布者识别，保留本人转发及其中的安全外部视频/网页链接。
- [x] `P0` 将连续分页缩小为 20 条并增加间隔，对中途游标的 `-10001` 只做一次延迟重试。
- [x] `P0` 失败或取消前的部分成果登记到当前账号，并可立即从“我的档案”读取。
- [x] `P0` 解析器升级为版本 4，补充转发、外链、分页和有限重试回归测试。

### 开发阶段：发布者字段与部分完成修复

- [x] `P0` 修正 scope=1 的发布者字段优先级，区分本人发布者与转发原作者。
- [x] `P0` 迁移回滚和主进程索引均以磁盘实际记录数为准，避免“文件存在但显示 0 条”。
- [x] `P0` 后续页被拒绝时尝试 scope=0 兼容读取；仍失败则合并旧档案并明确标记部分完成。
- [x] `P0` 新增发布者回归、迁移合并、磁盘计数和部分完成桌面流程测试。

### 开发阶段：按分类采集与好友流隔离

- [x] `P0` 说说改为优先读取本人“说说”分类，使用总数和数值偏移分页。
- [x] `P0` 禁止个人归档访问 scope=0 好友动态流；限流时最多退到 scope=1 本人时间线。
- [x] `P0` 解析器版本升级到 5，覆盖图文、转发正文、安全外链、评论回复和统计字段。
- [x] `P0` 进度显示说说分类页数与总条数，不再用好友流页数误导用户。

### 开发阶段：账号资料与图片查看器

- [x] `P0` 顶部账号入口显示本人 QQ 头像与昵称，账号列表显示完整 QQ 号。
- [x] `P0` 从 QQ 空间资料与已有档案中安全补全本人身份，临时登录 Cookie 仍在采集结束后清除。
- [x] `P1` 大图严格适配窗口安全区，支持滚轮缩放、拖动、复位和多图切换。
- [x] `P1` 修复查看器按钮点击层、箭头居中和极限拖动边界。

### 开发阶段：准确性、互动详情与大账号性能

- [x] `P0` 点赞/评论总数与已展开详情分开展示，不再用详情数组长度冒充总数。
- [x] `P0` 统一归档、界面和 AI 中的评论作者字段。
- [x] `P0` 媒体索引改为内存缓存与批量原子落盘。
- [x] `P0` 汇总统计改为增量索引，不再每页重读全部记录。
- [x] `P0` 档案读取分页化，时间线加入虚拟滚动。
- [x] `P0` 加入 1 万条动态、5 万张图片的压力测试。
- [x] `P0` 建立兼容性矩阵，覆盖自动回归并记录当前大小账号真实观察；正式发布前的人工验证门槛继续保留。
- [x] `P1` 增加采集适配器健康状态与可理解的兼容性错误提示。

## v0.4.1-alpha：当前功能收尾

- [x] 修正浏览器演示版本号与手动查看新版本的操作文案。
- [x] 收紧首页、空档案及账号删除范围说明。
- [x] 容忍旧档案未知类型，并过滤损坏或非 HTTPS 的外部链接。

## v0.5.0-alpha：导出与产品边界

- [x] `P1` 实现单文件离线 HTML 导出，支持原图、压缩副本和不含配图策略。
- [x] `P1` 实现 A4 PDF 导出，并通过中文文本、分页、图片和页面边界回归。
- [x] `P1` 实现 DOCX 导出；已通过标准 OOXML 结构检查和 Microsoft Word 实际打开/渲染，WPS 仍待人工兼容验证。
- [x] `P1` 加入全部档案、当前筛选、日期范围、互动开关、匿名化和可见人员信息二次确认。
- [x] `P1` 匿名编号按单篇内容独立生成，同篇评论者、点赞者和 `@昵称` 使用一致编号。
- [x] `P1` 离线导出将 QQ 表情代码转换为语义名称，未知代码保留原编号。
- [x] `P1` 明确首个正式版只承诺说说及说说配图；相册、视频和独立日志适配器继续标记为未完成。
- [ ] `P2` 在找到稳定只读接口后实现完整相册采集。
- [ ] `P2` 评估独立日志和视频适配器。

## v0.6.0-alpha：安全、无障碍与发行

- [x] `P1` 添加 CSP，并精确校验应用内部 URL。
- [x] `P1` 媒体下载增加 Content-Length、流式大小限制、安全位图类型和最终重定向域名复验。
- [x] `P1` 主窗口默认拒绝不需要的 Electron 权限，并禁止 WebView 与拖放导航。
- [x] `P1` 对话框加入焦点圈定与关闭后焦点恢复；完成键盘焦点、强制高对比度和 200% 缩放自动回归。
- [x] `P1` 将 8–10px 小字提高到最低 11px，并补充焦点与强制颜色样式。
- [x] `P1` 配置正式应用图标；Release 工作流预留 Windows 证书密钥接入。
- [x] `P1` 实现官方 GitHub Release 元数据检查，保持手动下载，避免未签名环境下静默替换程序。
- [x] `P1` CI 自动生成安装版、免安装版、SHA-256、CycloneDX SBOM 和生产依赖许可证清单。
- [x] `P1` 增加低磁盘启动保护、中文路径与当前用户免管理员安装配置和自动回归。
- [ ] `P1` 取得并接入可信 Windows 代码签名证书；签名完成前不启用自动安装更新。
- [ ] `P1` 在真实 Windows 10、Windows 11 干净系统及 WPS 上完成人工验收；当前自动化不能替代这些环境。

## v0.6.x：去除 Alpha 的基本可用门槛

- [ ] 连续多轮真实账号回归不存在数据丢失、不可恢复错误或重复爆增。
- [ ] 安装版与免安装版均经过干净系统验证，可读取旧版本归档并能安全回滚。
- [ ] 当前承诺范围内的所有 P0 项完成。

## v1.0.0：正式版本门槛

- [ ] 所有 P0、P1 项完成或在发行说明中明确降级范围。
- [ ] 隐私说明、QQ 非官方关联声明、支持范围和安全报告流程完整。
